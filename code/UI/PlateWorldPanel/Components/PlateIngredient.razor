@using Sandbox;
@using Sandbox.UI;
@inherits Panel
@namespace Undercooked.UI.PlateWorldPanel

<root>
    <Image Texture="@Ingredient.Icon" />
</root>

@code {
    [Property]
    public IngredientResource Ingredient { get; set; } = null!;

    protected override int BuildHash() => System.HashCode.Combine(Ingredient);
}